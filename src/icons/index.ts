/**
 * Контракт значка и системные глифы (DS-145).
 *
 * ГРАНИЦА: это `src/icons/`, а НЕ `src/components/Icon/`. Каталог компонентов
 * детектится как `git ls-tree -d src/components`, и папка там подняла бы релиз
 * до minor — тогда как контракт ничего в каталог не добавляет. Тем же доводом
 * живут рядом `src/dictionary/` и `src/theme/`.
 *
 * ЧТО ЗДЕСЬ ПУБЛИЧНО И ПОЧЕМУ ИМЕННО ЭТО. Семь глифов — НАША хромировка:
 * шеврон раскрытия, крестик закрытия, лупа поиска, «⋯» переполнения. Мы их уже
 * рисуем и обязаны держать; потребителю тот же шеврон нужен, когда он собирает
 * своё раскрытие руками, и до этой задачи он был вынужден рисовать копию — то
 * есть ровно то, что гейт `inline-icons` запрещает у нас самих.
 *
 * Это не пак и паком не станет: пополнять список по заявкам «а такой иконки
 * нет» — значит заводить таксономию навсегда и делать систему узким местом в
 * продуктовой работе потребителя. Новый глиф появляется здесь тогда и только
 * тогда, когда ЕГО РИСУЕТ КОМПОНЕНТ СИСТЕМЫ.
 */
export { Icon } from './Icon.js'
export type { IconProps } from './Icon.js'
export {
  ChevronDown, ChevronRight, ChevronLeft, ChevronUp,
  Close, Search, Dots, ToneIcon,
} from './glyphs.js'
export type { GlyphProps, ToneIconProps, ToneName } from './glyphs.js'
