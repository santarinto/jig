import { Prose, Card } from '@santarinto/jig'

/**
 * Готовый html — так его обычно и отдаёт бэкенд (markdown → html).
 * Санитайзить обязан источник: компонент ставит строку как есть.
 */
const ARTICLE = [
  '<h2>Заявка №1024</h2>',
  '<p>Тело задачи, отрендеренное на сервере из markdown. Ссылка на <a href="#">регламент</a>, список:</p>',
  '<ul><li>Проверить реквизиты контрагента</li><li>Согласовать сумму с руководителем</li></ul>',
  '<pre><code>make release VERSION=1.30.2 &amp;&amp; make push  # длинная строка кода скроллится внутри блока, а не распирает контейнер</code></pre>',
  '<blockquote>Важно: общие эндпоинты не трогаем до конца миграции.</blockquote>',
  '<table><thead><tr><th>Поле</th><th>Значение</th></tr></thead>',
  '<tbody><tr><td>ИНН</td><td>7701234567</td></tr><tr><td>Сумма</td><td>128 400 ₽</td></tr></tbody></table>',
].join('')

export const ServerHtml = () => (
  <div style={{ width: 420 }}>
    <Prose html={ARTICLE} />
  </div>
)

/** Тот же типографический контейнер, но содержимое — обычные React-дети. */
export const Children = () => (
  <div style={{ width: 420 }}>
    <Prose>
      <h3>Регламент проведения</h3>
      <p>
        Документ проводится после согласования суммы. Если контрагент новый —
        сначала <a href="#">проверка реквизитов</a>.
      </p>
      <ol>
        <li>Заполнить шапку</li>
        <li>Добавить позиции</li>
        <li>Провести</li>
      </ol>
    </Prose>
  </div>
)

/** В карточке: описание задачи под заголовком — самый частый случай. */
export const InCard = () => (
  <div style={{ width: 420 }}>
    <Card title="Задача TK-418" subtitle="описание из трекера">
      <Prose html={'<p>Резолв конфликта <code>merge develop</code> в контроллере API. Ветка задачи генерирует спеки «на лету», develop — отрефакторил пути.</p><ul><li>Сохранить фичу</li><li>Не потерять рефакторинг</li></ul>'} />
    </Card>
  </div>
)
